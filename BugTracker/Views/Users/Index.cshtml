@model IEnumerable<Users>

<h2>Users</h2>
<table class="table">
    <tr>
        <th>ID</th>
        <th>First Name</th>
        <th>Last Name</th>
        <th>email</th>
        <th>Title</th>
    </tr>

    @foreach (var user in Model)
    {
<tr>
    <td>@user.UserID</td>
    <td>@user.FirstName</td>
    <td>@user.LastName</td>
    <td>@user.emal</td>
    <td>@user.Title</td>
</tr>

    }
</table>
